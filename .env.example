VITE_API_BASE_URL=http://localhost:3001/api

# AI Invoice OCR service base URL (Aman). In dev this is the relative
# /ocr-api path proxied by Vite; point it directly at the service if not proxying.
VITE_OCR_API_BASE_URL=/ocr-api
OCR_API_PROXY_TARGET=https://catalog-footing-hunger.ngrok-free.dev
